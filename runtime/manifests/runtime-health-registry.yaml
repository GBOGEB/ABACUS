runtime_health_registry:
  moniker: DELTA_1
  phase: DELTA_1_PHASE_15_SELF_HEALING_GOVERNANCE

  domains:
    ci_cd:
      state: monitored
    security:
      state: monitored
    release:
      state: monitored
    deployment:
      state: monitored
    runtime_verification:
      state: monitored
    governance:
      state: monitored

  health_requirements:
    telemetry_required: true
    provenance_required: true
    governance_alignment_required: true

  objectives:
    - runtime federation health visibility
    - governance-runtime continuity
    - recursive operational awareness
