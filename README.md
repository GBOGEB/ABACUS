# GBOGEB/ABACUS

**Deterministic Engineering Publication Compiler**

> *"Generated outputs are NEVER canonical."*

GBOGEB/ABACUS is a governed publishing platform that transforms design blueprints from GBOGEB/CODEX into deterministic, accessible, and traceable engineering publications with immutable lineage tracking.

---

## Hub & Downloads

| Resource | Location |
|----------|----------|
| 🌐 **Hub Landing Page** | [`web/index.html`](web/index.html) — central navigation with topology cards |
| 📦 **Workspace Bundle** | `workspace_bundle.tar.gz` — generated by CI/CD ([archive-deploy workflow](.github/workflows/archive-deploy.yml)) |
| 🔬 **Multi-View Engineering Tool** | [`examples/multi_view_workspace/`](examples/multi_view_workspace/) — interactive SPA |
| 🗺️ **Knowledge Topology** | [`config/knowledge_topology.json`](config/knowledge_topology.json) — cross-repo node map |
| 📋 **Stakeholder Registry** | [`config/stakeholder_registry.yaml`](config/stakeholder_registry.yaml) — communication routes |

---

## Quick Start

### Prerequisites

- Python 3.11+
- Ruby 3.0+ (for Jekyll)
- Git 2.30+

### Setup

```bash
# Clone the repository
git clone https://github.com/GBOGEB/ABACUS.git
cd ABACUS

# Install Python dependencies
pip install pyyaml pytest pytest-cov

# Install Jekyll dependencies
bundle install

# Verify installation
python engines/RENDER_LINTER.py --help
python engines/WCAG_CONTRAST_CHECKER.py --help
python engines/SLIDE_ID_ENFORCER.py --help
python engines/verification_hook.py --help
```

### Process New Assets

```bash
# Drop binary files into Input_Master/
cp presentation.pptx Input_Master/

# Run verification hook
python engines/verification_hook.py

# Check manifest status
python engines/verification_hook.py --status
```

### Run Governance Checks

```bash
# Lint all content
python engines/RENDER_LINTER.py docs/

# Check contrast compliance
python engines/WCAG_CONTRAST_CHECKER.py --theme engines/SEMANTIC_THEME.yaml

# Validate slide IDs
python engines/SLIDE_ID_ENFORCER.py docs/

# Run all tests (79 passing)
python -m pytest tests/ -v
```

### Run the Engineering Example

```bash
# Physics validator
cd examples/multi_view_workspace/output/context
python physics_validator.py

# Generate PDF report (choose theme: userstyle | corporate | focus)
cd ../../..
python examples/multi_view_workspace/scripts/generate_pdf.py --theme corporate

# Open the interactive SPA
open examples/multi_view_workspace/output/documents/index.html
```

### Build Site

```bash
bundle exec jekyll serve
```

---

## Architecture

| Repository | Role |
|------------|------|
| **GBOGEB/CODEX** (GBC) | Design & theme blueprint — canonical source of truth |
| **GBOGEB/ABACUS** (GBA) | Governance & processing engine — this repo |

See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) for the complete system architecture.

### Cross-Repository Topology

The **knowledge topology** ([`config/knowledge_topology.json`](config/knowledge_topology.json)) maps all nodes across GBA and GBC with their dependencies:

```
GBC (CODEX)                          GBA (ABACUS)
┌──────────────┐                     ┌──────────────────────────────────┐
│ Design       │  binary export      │  Input Gate                     │
│ Tokens  ─────┼──────────────────→  │  ├─ verification_hook.py        │
│ Layout  ─────┼──────────────────→  │  ├─ lineage_manifest.json       │
│ Blueprints   │  theme sync         │  │                              │
│ Exports ─────┼──────────────────→  │  A6 Governance Engines          │
└──────────────┘                     │  ├─ RENDER_LINTER.py            │
                                     │  ├─ WCAG_CONTRAST_CHECKER.py    │
                                     │  ├─ SLIDE_ID_ENFORCER.py        │
                                     │  ├─ SEMANTIC_THEME.yaml         │
                                     │  └─ LAYOUT_CONTRACTS.yaml       │
                                     │                                  │
                                     │  Output Renderers                │
                                     │  ├─ Jekyll (HTML)               │
                                     │  ├─ Multi-View SPA              │
                                     │  └─ PDF (WeasyPrint)            │
                                     │                                  │
                                     │  Web Hub                         │
                                     │  └─ web/index.html              │
                                     └──────────────────────────────────┘
```

### Repository Structure

```
GBOGEB/ABACUS
├── .github/workflows/
│   ├── governance-validation.yml    ← Lint, contrast, slide-IDs, tests
│   ├── asset-verification.yml       ← Binary asset SHA256 verification
│   └── archive-deploy.yml           ← Bundle generation + GitHub Pages
├── _data/lineage_manifest.json      ← Asset lineage registry
├── _layouts/{default,slide}.html    ← Jekyll templates
├── assets/{css,js}/                 ← Semantic stylesheet + theme switcher
├── config/
│   ├── governance_tuning.yaml       ← Governance rule configuration
│   ├── knowledge_topology.json      ← Cross-repo node mapping
│   └── stakeholder_registry.yaml    ← Communication routes
├── docs/                            ← Architecture, bridge, routing docs
├── engines/                         ← A6 Governance Engines (8 files)
├── examples/multi_view_workspace/   ← Engineering Tool SPA
│   ├── output/context/              ← YAML SSOT + Python validator
│   ├── output/documents/            ← HTML, MD, PDF, plaintext outputs
│   └── scripts/                     ← PDF generation
├── tests/                           ← 79 unit + integration tests
├── web/                             ← Hub landing page
│   ├── index.html                   ← Central navigation hub
│   └── static/{css,js}/             ← Hub styling + interactivity
└── Input_Master/                    ← Binary asset drop zone
```

---

## A6 Governance Engines

| Engine | File | Purpose |
|--------|------|---------|
| Render Rules | `engines/RENDER_RULES.md` | Comprehensive governance ruleset |
| Semantic Theme | `engines/SEMANTIC_THEME.yaml` | Light/dark/high-contrast tokens |
| Layout Contracts | `engines/LAYOUT_CONTRACTS.yaml` | Deterministic spacing constraints |
| Render Linter | `engines/RENDER_LINTER.py` | ESLint-style content validation |
| Lineage Schema | `engines/LINEAGE_SCHEMA.yaml` | Immutable traceability schema |
| Contrast Checker | `engines/WCAG_CONTRAST_CHECKER.py` | WCAG AA compliance |
| Slide ID Enforcer | `engines/SLIDE_ID_ENFORCER.py` | Deterministic ID validation |
| Verification Hook | `engines/verification_hook.py` | Binary asset processing |

## Stakeholder Routing

Output delivery is routed based on audience needs ([`config/stakeholder_registry.yaml`](config/stakeholder_registry.yaml)):

| Tag | Audience | Preferred Format |
|-----|----------|-----------------|
| `[DOW]` | Engineering teams | Plaintext, code, YAML, interactive HTML |
| `[KEB]` | Project leadership | Slides, dashboards, summary PDFs |
| `[ALL]` | Review boards | Formal PDFs with lineage + governance evidence |

See [docs/STAKEHOLDER_ROUTING.md](docs/STAKEHOLDER_ROUTING.md) for the full distribution logic.

## CI/CD Pipelines

| Workflow | Trigger | Purpose |
|----------|---------|---------|
| `governance-validation.yml` | PR / push to main | Lint, contrast, slide-IDs, tests |
| `asset-verification.yml` | Changes to Input_Master/ | SHA256 verification + manifest |
| `archive-deploy.yml` | Push to main / manual | Bundle `.tar.gz` + deploy to GitHub Pages |

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for the development workflow.

## License

Proprietary — GBOGEB Governance Board



---

## 🔌 MCP Server Integration

This repository is connected to the **GBOGEB MCP Server** — a [Model Context Protocol](https://modelcontextprotocol.io/) server that gives AI assistants unified access to the full GBOGEB knowledge topology, engineering data, and governance engines across GBA and GBC.

### Quick Setup

```bash
cd /home/ubuntu/gbogeb_mcp_server
pip install -e ".[dev]"
gbogeb-mcp  # starts the MCP server (stdio transport)
```

### Available MCP Tools

| Tool | Description |
|---|---|
| `get_topology()` | Full merged knowledge graph (20 GBA + 7 GBC nodes) |
| `query_node(id)` | Node details with edges and file preview |
| `search_engineering_data(q)` | Full-text search across YAML/JSON data |
| `get_asset_lineage(id)` | SHA256 provenance for tracked assets |
| `validate_physics(data)` | He-4 thermodynamic validation via HeliumPropertyEngine |
| `list_assets()` | All tracked SVG/binary assets |
| `get_test_status()` | Live pytest results for both repos |

See [`gbogeb_mcp_server/README.md`](/home/ubuntu/gbogeb_mcp_server/README.md) for full documentation.
