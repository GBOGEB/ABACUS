name: W64 Census P3 Receipt

on:
  pull_request:
    paths:
      - 'tools/w64_total_repo_census.py'
      - 'tools/w64_census_reverse_pressure.py'
      - 'tools/w64_census_p3_receipt.py'
      - '.github/workflows/w64-census-p3.yml'
  workflow_dispatch:

jobs:
  census:
    runs-on: ubuntu-latest
    env:
      W64_RECEIPTS: architecture/w64/receipts
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run P1 census
        run: |
          mkdir -p "$W64_RECEIPTS"
          p1="$W64_RECEIPTS/W64_TOTAL_REPO_CENSUS_P1.json"
          python tools/w64_total_repo_census.py \
            --root . \
            --out "$p1"
      - name: Run corrected P2 reverse pressure
        run: |
          p1="$W64_RECEIPTS/W64_TOTAL_REPO_CENSUS_P1.json"
          p2="$W64_RECEIPTS/W64_TOTAL_REPO_CENSUS_P2_REVERSE_PRESSURE.json"
          python tools/w64_census_reverse_pressure.py \
            --census "$p1" \
            --out "$p2"
      - name: Emit P3 receipt
        run: |
          p1="$W64_RECEIPTS/W64_TOTAL_REPO_CENSUS_P1.json"
          p2="$W64_RECEIPTS/W64_TOTAL_REPO_CENSUS_P2_REVERSE_PRESSURE.json"
          p3="$W64_RECEIPTS/W64_TOTAL_REPO_CENSUS_P3_RECEIPT.json"
          python tools/w64_census_p3_receipt.py \
            --p1 "$p1" \
            --p2 "$p2" \
            --out "$p3"
      - name: Show P3 status
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          root = Path('architecture/w64/receipts')
          p = root / 'W64_TOTAL_REPO_CENSUS_P3_RECEIPT.json'
          r = json.loads(p.read_text())
          print(json.dumps({
              'status': r['status'],
              'release_credit': r['release_credit'],
              'w64_dov': r['w64_dov'],
              'p2_blocker_count': r['p2']['blocker_count'],
              'p2_warning_count': r['p2']['warning_count'],
              'finding_types': r['p2']['finding_types'],
          }, sort_keys=True))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: w64-census-p1-p2-p3-receipts
          path: architecture/w64/receipts/
