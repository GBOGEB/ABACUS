name: Docs Build
on:
  push:
    paths:
      - 'docs/**'
jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'Build documentation portal and review assets'
