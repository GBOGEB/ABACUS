# ABACUS publication contract

ABACUS records that an accepted private release exists; it does not host that
release. A valid pointer records the release ID, semantic private-source hash,
QA count, release-manifest hash, artifact-registry hash and OneDrive-relative
publication path.

The public pointer must not include current bidder prices, source offer text,
private file names or OneDrive sharing tokens.
