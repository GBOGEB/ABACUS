# ABACUS Docs (docs/)

This folder contains a GitHub Pages-ready documentation site for ABACUS. It includes conceptual, procedural, and reference articles and a manifest for automation.

To publish:
- Enable GitHub Pages in repository settings and point Pages to the `docs/` folder on the default branch.

To create a distributable zip, use the Makefile at repo root: `make docs-zip`.
