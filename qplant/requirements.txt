numpy==1.24.3
pandas==2.0.2
plotly==5.14.1
scipy==1.10.1
pytest==9.0.3
pytest-cov==4.1.0
PyYAML==6.0.2

# QPLANT API runtime and executable contract tests.
# Keep these as bounded major-version ranges so the service gets compatible
# FastAPI/Starlette/Pydantic resolution without silently crossing major APIs.
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
