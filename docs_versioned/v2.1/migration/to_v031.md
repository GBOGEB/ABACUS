# Migration Guide: v2.1 → v0.31
> *Reconstructed from code — 2026-05-16 22:34*

## Overview
v0.31 establishes the **canonical foundation** that all subsequent versions build upon.

## What Changes
1. **Canonical Indexes** — `canonical.index.json` becomes the machine-readable artifact registry
2. **DOW Engine Config** — `dow_engine_config.yaml` formalizes DOW pipeline configuration
3. **Artifact Rankings** — `artifact_rankings.json` introduces quality scoring

## Migration Steps
1. Preserve all v2.1 artifacts (they remain referenced)
2. Import canonical index from `ABACUS-v031/canonical.index.json`
3. Configure DOW engine via `ABACUS-v031/dow_engine_config.yaml`
4. Validate with `ABACUS-v031/run_direct_improvements.py`

## Breaking Changes
- None. v0.31 is additive — it establishes new canonical references without removing v2.1 content.

## Deprecation Notes
- v2.1 deployment scripts remain functional but v0.31 canonical config supersedes them for new work.
