<<<<<<< HEAD
# =============================================================================
# MINERVA P&ID — CI / CD test & reproducibility pipeline
# =============================================================================
# STAGED LOCATION NOTICE
# ----------------------
# This file lives at `ci/minerva-pid-test.yml` because the connected Abacus
# GitHub App currently lacks the `workflows` permission and therefore cannot
# push files under `.github/workflows/`. A maintainer must land it:
#
#     git mv ci/minerva-pid-test.yml .github/workflows/minerva-pid-test.yml
#     git commit -m "ci: activate MINERVA P&ID workflow" && git push
#
# See docs/W007_MANUAL_SETUP.md for full instructions.
# =============================================================================

name: MINERVA P&ID — Test & Reproducibility

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

# Read-only token; CI reports status, humans merge (no write-back / auto-merge).
permissions:
  contents: read

concurrency:
  group: minerva-pid-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: build + test (py${{ matrix.python-version }})
=======
# MINERVA QCELL/RFCELL P&ID subproject — regenerate + test + coverage + golden gate (W007)
# Scoped to MINERVA_PID/** so it never interferes with other ABACUS subprojects.
#
# DEPLOYMENT NOTE: this file lives at MINERVA_PID/ci/minerva-pid-test.yml because
# the integration bot lacks the GitHub `workflows` permission and cannot push
# under .github/workflows/. A maintainer activates it once with:
#
#     cp MINERVA_PID/ci/minerva-pid-test.yml .github/workflows/minerva-pid-test.yml
#     git add .github/workflows/minerva-pid-test.yml && git commit && git push
#
# CONVENTION NOTE: this project deliberately does NOT use pytest. Tests are
# standalone runners (`python3 tests/test_*.py`). Coverage is collected by
# wrapping those runners with `coverage run -p` (the pytest-cov-free equivalent),
# never by invoking `pytest`.
name: MINERVA P&ID — build, test & coverage

on:
  push:
    paths:
      - "MINERVA_PID/**"
      - ".github/workflows/minerva-pid-test.yml"
  pull_request:
    paths:
      - "MINERVA_PID/**"
      - ".github/workflows/minerva-pid-test.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: MINERVA_PID

jobs:
  build-and-test:
    name: build & test (py${{ matrix.python-version }})
>>>>>>> origin/main
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

<<<<<<< HEAD
    defaults:
      run:
        working-directory: MINERVA_PID

    env:
      PYTHONPATH: src

    steps:
      # ----------------------------------------------------------------------
      - name: Checkout
        uses: actions/checkout@v4

      # ----------------------------------------------------------------------
=======
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native libraries (CairoSVG, lxml, PDF tooling)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcairo2 libcairo2-dev \
            poppler-utils \
            libxml2-dev libxslt1-dev

>>>>>>> origin/main
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
<<<<<<< HEAD
          cache: pip
          cache-dependency-path: MINERVA_PID/requirements.txt

      # ----------------------------------------------------------------------
      # System deps:
      #   - libcairo2-dev : cairosvg (SVG -> PDF/PNG rasterisation, atlas v6)
      #   - poppler-utils : PDF tooling used by report/preview generation
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends poppler-utils libcairo2-dev

      # ----------------------------------------------------------------------
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ----------------------------------------------------------------------
      # Reproducibility: regenerate EVERY derived artifact from tracked source.
      # `make.sh` is itself a 7-stage integration test and must exit 0.
      - name: Regenerate all artifacts (reproducibility — make.sh)
        run: |
          ./make.sh --clean   # wipe derived outputs, then full regeneration
          ./make.sh           # second pass confirms idempotent reproducibility

      # ----------------------------------------------------------------------
      # Test battery: standalone runners (NO pytest). Each tests/test_*.py uses
      # assert + _run_all() and self-skips data-dependent suites if needed.
      # Falls back to `pytest` automatically if it is available in the env.
      - name: Run full test suite (standalone runners)
        run: |
          set -e
          fail=0
          for t in tests/test_*.py; do
            echo "::group::$t"
            if python3 "$t"; then
              echo "PASS  $t"
            else
              echo "::error file=$t::test runner returned non-zero"
              fail=1
            fi
            echo "::endgroup::"
          done
          if [ "$fail" -ne 0 ]; then
            echo "One or more test runners failed."
            exit 1
          fi

      # Optional pytest pass (collects the same assert-based tests). Non-fatal:
      # the standalone runners above are the source of truth.
      - name: Coverage report (optional pytest + coverage)
        continue-on-error: true
        run: |
          pip install pytest pytest-cov coverage || exit 0
          PYTHONPATH=src pytest tests/ \
            --cov=src/abacus_svg_pid \
            --cov-report=term-missing \
            --cov-report=xml:reports/coverage.xml || true

      # ----------------------------------------------------------------------
      # Golden-file gate: committed reports/*_statistics.json are golden
      # snapshots. Compare regenerated stats semantically. Fail on count drift;
      # ignore the documented ~1-byte XLSX zip jitter (xlsx excluded entirely).
      - name: Golden-file comparison gate
        run: |
          python3 ci/golden_gate.py

      # ----------------------------------------------------------------------
      - name: Upload build artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: minerva-pid-artifacts-py${{ matrix.python-version }}
          if-no-files-found: ignore
          retention-days: 14
          path: |
            MINERVA_PID/publish/*.html
            MINERVA_PID/reports/*.md
            MINERVA_PID/reports/*_statistics.json
            MINERVA_PID/data/crossmap/*.json
            MINERVA_PID/reports/coverage.xml
=======

      - name: Install Python dependencies (exact pins) + coverage.py
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install coverage

      - name: Verify CairoSVG native binding
        run: python -c "import cairosvg; print('cairosvg', cairosvg.__version__)"

      - name: Regenerate all derived outputs (make.sh)
        run: |
          chmod +x make.sh
          ./make.sh

      - name: Run test suite under coverage (standalone runners — no pytest)
        run: |
          export PYTHONPATH=src
          rm -f .coverage .coverage.* coverage.xml
          fail=0
          for t in tests/test_*.py; do
            echo "::group::$t"
            # `coverage run -p` (parallel mode) wraps each standalone runner;
            # a non-zero exit from the runner is captured as a test failure.
            coverage run -p "$t" || fail=1
            echo "::endgroup::"
          done
          if [ "$fail" -ne 0 ]; then
            echo "::error::One or more MINERVA P&ID test files failed."
            exit 1
          fi

      - name: Combine + report coverage
        run: |
          coverage combine
          coverage report
          coverage xml
          # Re-print a compact total line into the job summary.
          echo "### Coverage (py${{ matrix.python-version }})" >> "$GITHUB_STEP_SUMMARY"
          coverage report --format=markdown >> "$GITHUB_STEP_SUMMARY" || \
            coverage report >> "$GITHUB_STEP_SUMMARY"

      - name: Golden-file regression gate
        run: |
          export PYTHONPATH=src
          # Dedicated standalone runner comparing make.sh outputs against the
          # committed tests/golden/ snapshots (counts, ids, roles, histograms —
          # NOT volatile geometry/XLSX bytes, so the gate is deterministic).
          python3 tests/test_golden_files.py

      - name: Reproducibility check (make.sh --clean is idempotent)
        run: |
          # A clean rebuild must reproduce the key derived artifacts. The known
          # ~1-byte XLSX jitter is intentionally excluded from the golden gate,
          # so this step only asserts the artifacts exist after a clean rebuild.
          ./make.sh --clean
          missing=0
          for f in \
            data/model/line_model.json \
            data/excel/canonical_register_v1.yaml \
            data/excel/catalog_register.json \
            reports/COMPONENT_CATALOG_v2.xlsx; do
            if [ ! -f "$f" ]; then
              echo "::error::reproducibility: expected artifact missing after clean rebuild: $f"
              missing=1
            else
              echo "ok: $f"
            fi
          done
          [ "$missing" -eq 0 ]

      - name: Re-run golden gate after clean rebuild
        run: |
          export PYTHONPATH=src
          python3 tests/test_golden_files.py

      - name: Upload build + coverage artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: minerva-pid-py${{ matrix.python-version }}
          path: |
            MINERVA_PID/coverage.xml
            MINERVA_PID/reports/*.md
            MINERVA_PID/reports/*.json
            MINERVA_PID/publish/*.html
          if-no-files-found: warn
          retention-days: 14

      # OPTIONAL — Codecov upload. Requires a CODECOV_TOKEN repository secret
      # configured by a maintainer; left commented so the pipeline is green
      # without external service setup.
      # - name: Upload coverage to Codecov
      #   uses: codecov/codecov-action@v4
      #   with:
      #     files: MINERVA_PID/coverage.xml
      #     flags: minerva-pid
      #     token: ${{ secrets.CODECOV_TOKEN }}
>>>>>>> origin/main
