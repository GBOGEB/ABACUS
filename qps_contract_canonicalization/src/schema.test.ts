import test from "node:test";import assert from "node:assert/strict";import {Ssot} from "./schema.js";const r={id:"A",canonicalId:"C",semanticGroupKey:"G",applicability:"APPLICABLE" as const,complianceStatus:"OPEN" as const,scoreEligible:true,reviewWeight:1,scopeTags:[],overrideEvidence:[]};const base={schemaVersion:"1.0.0" as const,removedScopeTags:[],outwardArtifacts:[{id:"HTML",dependsOn:["ssot"]}]};test("no duplicate scoring",()=>assert.equal(Ssot.safeParse({...base,rows:[r,{...r,id:"B"}]}).success,false));test("no obsolete resurrection",()=>assert.equal(Ssot.safeParse({...base,removedScopeTags:["QSN"],rows:[{...r,scopeTags:["QSN"]}]}).success,false));test("obsolete must be neutral",()=>assert.equal(Ssot.safeParse({...base,rows:[{...r,applicability:"OBSOLETE"}]}).success,false));
