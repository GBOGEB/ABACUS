import fs from "node:fs";import crypto from "node:crypto";import {Ssot} from "./schema.js";const p=new URL("../ssot/review_projection.json",import.meta.url);const raw=fs.readFileSync(p,"utf8");const s=Ssot.parse(JSON.parse(raw));const score=s.rows.filter(r=>r.scoreEligible);const leak=score.filter(r=>r.scopeTags.some(t=>s.removedScopeTags.includes(t)));if(leak.length)throw new Error("obsolete QSN/LN2 scope leaked into artifact population");console.log(JSON.stringify({validated:true,sha256:crypto.createHash("sha256").update(raw).digest("hex"),sourceRows:s.rows.length,denominatorCount:score.length,excluded:s.rows.length-score.length,outwardArtifacts:s.outwardArtifacts.map(a=>a.id)},null,2));
