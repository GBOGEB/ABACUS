# Slide images

Slide images are generated at runtime. Run `yarn prisma db seed` after seeding the database to populate this folder.

The application expects 15 PNG files named `slide-01.png` … `slide-15.png` in this directory. They are intentionally not committed to the repository (large binary artefacts).
