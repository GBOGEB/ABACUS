# W83-NG1 non-governance outcome diagnostic

This MC-2 continuation consumes already-executed exact-source W83 typed-basis
receipts for the eleven W80 states that have independent observed validation
outcomes.

Only non-governance feature blocks are eligible:
- semantic composition;
- consumer-graph distribution.

Workflow-governance composition is excluded because W83 already showed that its
recent variation carries zero outcome information and the next frontier explicitly
requires non-governance variation.

Geometry is small: n=11, 8 docs failures and 3 passes. Every varying
non-governance feature is tested using an exact two-sided label-permutation test on
absolute AUC distance from chance. All 165 possible 8-of-11 failure assignments are
enumerated. Holm correction controls family-wise multiplicity across the varying
feature set.

This is ADVANCED exact inference under the reusable math skill contract. It is
diagnostic only: no PCA is fit, no causal effect is claimed, no pairwise event is
admitted, and Bradley-Terry/allocation/engineering authority remain withheld.
