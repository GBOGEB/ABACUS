# W79 restart predicate

Continue W79 only from the first failed exact-head gate. Do not widen the method while a narrower execution or governance red exists.

Order:

1. exact PR-head checkout identity;
2. pinned NumPy + helper tests;
3. recompute the 15 exact W78 historical source states;
4. execute semantic-work PCA/PA95 and full-work sensitivity PCA/PA95;
5. bind run/job/runner/>0-step execution receipt;
6. upload retained receipt artifact;
7. satisfy ABACUS CI workflow governance.

On PASS, return the diagnostic receipt to MC-2/#1164 and stop. BT/reverse-BT remains a separate future gate requiring observed pairwise outcome evidence.
