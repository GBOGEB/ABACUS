<!-- markdownlint-disable MD013 -->

# MIP 3PC + MIP restart - 2026-09-14

## Preserve and pin

The prior review set is merged: ABACUS #1127, CODEX #631, DOCX #49.
Current observed main heads:
- ABACUS: `f431061038ec94299cee0a85dd4a7336e6a5bc4b`
- CODEX: `d79a1e21396a9556f23142e57bb9b58dd7921534`
- DOCX_RTM_Automation: `d30abebfd14d39f10921d3b9881d93c86e7b5309`

Canonical v1 acceptance is MIP/N2_CROSS_REPO_ACCEPTANCE_20260912.json:
8/8 gates, 4/4 surfaces, CONTROL. Do not reopen or extend that closed denominator.
MIP v2 has a separate fixed denominator of 10; no new v2 acceptance count is asserted here.
Broader/global DoV remains WITHHELD. Main triage/cryoplant retains final domain disposition.

## Prove and partition

Observed defect: Python bool subclasses int. Both KEB receipt and DOW challenge validators
accepted executed_steps=true for PASS, advancing toward downstream execution without a numeric count.
Reproduced both cases from pinned source. Repair requires type(value) is int and value > 0.
This preserves positive integer behavior and existing missing-evidence WAIT semantics.

Verification: all 8 plain controller test functions passed by direct stdlib/runpy invocation.
The added tests reject 7 malformed values at each of 2 boundaries (14 cases), and accept integer 1.
These are synthetic unit fixtures, not real producer/consumer/child proof.
Pytest is unavailable locally. This is a fetched source snapshot with necessary baseline files,
not a complete checkout; GitHub CI and accepted-head repetition remain separate gates.

## 3PC classification and MIP

Use the existing W109 3PC taxonomy: classification, authority, lifecycle, consumer, discrimination.
Classification: runtime evidence validation repair. Authority: ABACUS DOW intake/challenge.
Consumer: existing v2 controller. Lifecycle: reproduce -> repair -> prove -> review -> repeat.
Discrimination: bool is not count; fixture execution is not federated proof; v1 is not v2.
M: tighten positive-step type checks. I: exercise malformed evidence at both boundaries.
P: retain regression tests, immutable source identifiers, content hashes and restart sequence.

## Propagate and park

This pulse changes ABACUS only because the demonstrated defect is in its controller.
CODEX and DOCX heads are reconciled and pinned; no filler PRs or duplicate repairs are needed.
The proposed branch is mip/v2-step-count-handover-20260914.
Locate its PR by exact head; do not duplicate it if already open or merged.
No auto-merge is requested. Preserve all newer work when reconciling.

Priority and sequence:
1. Inspect PR review and exact-head CI; repair only the first actual failure.
2. After acceptance, repeat focused verification from merged source.
3. Review genuine KEB receipt binding before V2-G5 promotion. The current validator checks
   receipt_sha256 shape, not digest recomputation or external provenance; this remains a review gap.
4. Execute independent DOW challenge and child re-entry using real receipts; never substitute fixtures.
5. Feed disposition back to the controller. Obtain measured V2-G9 telemetry and V2-G10 distinct-SHA repeat.
6. Expand only in separate versioned scope with available capacity.

Default resources: two execution lanes and one verification lane, existing Python controller,
existing MIP v2 workflow, KEB/DOW/child contracts, no newly spawned agents or scheduler.
If a job never obtains a runner, classify it as infrastructure/DEFER; no speculative application repair.
PCA requires measured data and adequate samples. Use explicit manual BT ranking otherwise.

## Loss boundary and restart contract

MIP/RESTART_20260914.json contains the structured state, hashes, limitations and next actions.
Together with pinned repository source and PR history it preserves this pulse's operational state.
This is not a byte-for-byte archive of the whole conversation or unavailable private evidence.
Restart verification: resolve manifest -> pinned source -> compare hashes -> run tests -> inspect
PR/CI -> select next unmet gate. Never infer accepted-head proof from these local tests.

## Drop-in text

Continue MIP-V2-STEP-COUNT-20260914 from GBOGEB/ABACUS.
Read MIP/RESTART_20260914.json and .md on mip/v2-step-count-handover-20260914 or merged main.
Find the existing PR by that head, reconcile main and CI, and preserve newer work.
V1 is fixed 8/8 CONTROL across four surfaces. V2 is separate with ten gates; global DoV WITHHELD.
A boolean-as-executed_steps defect was reproduced and repaired in KEB and DOW PASS validation.
Eight plain test functions passed locally, including 14 malformed cases; pytest/CI proof is separate.
Verify the proposed/accepted head, then examine genuine receipt digest/provenance binding for V2-G5,
execute DOW challenge and child re-entry, and repeat on a distinct SHA before any v2 CONTROL claim.
Do not publish private receipts, fabricate runtime evidence, revive superseded branches or duplicate PRs.
